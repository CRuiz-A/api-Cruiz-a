{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "CAPTCHA_PASS_SECRET": "${CAPTCHA_PASS_SECRET}",
    "CAPTCHA_PASS_TTL": "15m"
  }
}
